# Streamlit Cloud Secrets
# Copy this to Settings > Secrets in Streamlit Cloud

# BOT API Credentials
BOT_CLIENT_ID = "your_client_id_here"
BOT_CLIENT_SECRET = "your_client_secret_here"

# Optional: Database path (for cloud deployment)
# DATABASE_URL = "etax.db"
